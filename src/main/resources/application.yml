
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/checkduplicate?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
mybatis:
  mapper-locations: classpath:/mapping/*.xml
  type-aliases-package: com.ruobai
logging:
  level:
    com:
      ruobai:
        repository: DEBUG
server:
  port: 8080